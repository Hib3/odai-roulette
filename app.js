const officialTopicText = `
最近うれしかった小さな出来事について
最近買ってよかったものと、その理由について
最近手放してよかったものについて
最近始めてみて意外だったことについて
最近やめてみて楽になったことについて
最近見つけたお気に入りの場所について
最近また食べたいと思ったものについて
最近誰かにすすめたくなったものについて
自分を少し褒めるならどこを選ぶかについて
最近の生活で変えてよかった習慣について
朝の時間を気持ちよくする工夫について
夜の時間を落ち着かせる工夫について
休日を充実させる過ごし方について
予定のない日にしたいことについて
忙しい日に自分を助けてくれるものについて
疲れた日に戻りたくなる場所について
気分転換のためにしている小さな工夫について
元気がない日に食べたいものについて
集中したいときに整える環境について
家で一番好きな場所について
自分の部屋で気に入っているところについて
机まわりでこだわっているものについて
かばんの中にいつも入れているものについて
スマホの中でよく使うものについて
ホーム画面に置いているものの理由について
毎日使っている道具について
長く使い続けているものについて
買わずにすませてよかったものについて
少し高くても選んでよかったものについて
自分へのごほうびにしたいことについて
好きな季節の楽しみ方について
雨の日を楽しくする方法について
暑い日にしたくなることについて
寒い日にしたくなることについて
季節の変わり目に気をつけていることについて
春になったら最初にしたくなることについて
夏の日に思い出す場所や出来事について
秋になると変わる自分の過ごし方について
冬にだけ楽しみにしていることについて
天気で気分が変わるときについて
思い出に残っている旅について
いつか行ってみたい場所について
もう一度行きたい場所について
近所で好きな道について
帰り道に寄りたくなる場所について
地元の好きなところについて
住んでみたい街について
知らない街でまず見たいものについて
旅先でつい買ってしまうものについて
移動時間の過ごし方について
好きな食べ物のこだわりについて
苦手だったけど好きになった食べ物について
朝に食べたいものについて
夜に食べたいものについて
疲れた日に食べる定番について
人に作ってもらってうれしかった料理について
自分で作れるとうれしい料理について
思い出に残っている外食について
コンビニでつい選ぶものについて
おやつにまつわる思い出について
好きな飲み物と飲みたい場面について
誰かと食べたいものについて
一人でゆっくり食べたいものについて
食べ方にこだわりがあるものについて
名前だけで気になる食べ物について
好きな本と出会ったきっかけについて
読み返したくなる本について
人にすすめたい本について
本を読む場所の好みについて
積んでいる本を選んだ理由について
忘れられない一文について
好きな漫画の魅力について
何度も見たい映画について
最近心に残った作品について
音楽を聴きたくなる瞬間について
自分だけのプレイリストを作るなら何を入れるかについて
昔よく聴いていた曲について
元気を出したいときの音について
静かに過ごしたいときの音について
写真に残したい日常について
カメラロールに残っている意外な一枚について
好きな色を選ぶ理由について
着ていると落ち着く服の理由について
髪型を変えたくなるタイミングについて
身につけると気分が上がるものについて
長く着ている服の思い出について
捨てられないものの理由について
部屋に飾りたいものについて
見ているだけで楽しいものについて
かわいいと思ったものについて
かっこいいと思ったものについて
昔好きだったものを今どう思うかについて
子どものころ夢中だった遊びについて
学生時代の思い出について
給食やお弁当にまつわる思い出について
初めてのアルバイトや仕事の思い出について
緊張したけどやってよかった経験について
失敗したけど今は笑える出来事について
忘れたいけど忘れられない出来事について
誰かに言われてうれしかった言葉について
言えなかったけど伝えたいことについて
ありがとうを伝えたい相手について
大切な人にすすめたいものについて
家族との小さな思い出について
友人との忘れられない会話について
性格の中で気に入っているところについて
扱いにくい自分の一面との付き合い方について
大人になったと感じた瞬間について
まだ子どもっぽいと思うところについて
自分の変わらないところについて
昔より変わったと思うところについて
これから伸ばしたいところについて
苦手だけど克服したいことについて
得意と言えるようになったことについて
続けていてよかったことについて
三日坊主になりがちなことについて
習慣にしたいことについて
やる気が出ない日の向き合い方について
集中できない日の逃げ道について
自分なりの休み方について
眠れない夜に考えることについて
早起きできた日にしたいことについて
予定を立てるときに大事にしていることについて
お金を使ってよかった経験について
節約してよかった経験について
貯めたいものと使いたいものについて
もし自由な一日があったら何をするかについて
もし長い休みがあったら何をするかについて
もし小さなお店を開くなら何を並べるかについて
もし別の仕事をするなら何をしたいかについて
もし昔の自分に一言だけ言えるなら何を言うかについて
もし明日から新しい趣味を始めるなら何を選ぶかについて
もし誰かと入れ替われるなら何を体験したいかについて
もし好きな場所に住めるならどこを選ぶかについて
もし自分の展示会を開くなら何を飾るかについて
もし一冊だけ本を持って旅に出るなら何を選ぶかについて
今いちばん必要だと感じているものについて
すでに十分持っていると感じるものについて
大切にしたい時間を一つ選ぶなら何かについて
暮らしの中で守り続けたいことについて
生活の中で少し変えたいことについて
今後試してみたいことについて
できるようになったらうれしいことについて
会って話してみたい人と聞きたいことについて
いつか自分の目で見たい景色について
作れるようになりたいものについて
誰かに教えられるくらい詳しくなりたいことについて
学んでみたら見方が変わったことについて
調べ始めたら止まらなかったことについて
気になる話題について自分なりに考えたことについて
以前と今で考え方が変わったことについて
ほっとした瞬間に何があったかについて
少し勇気を出して動いてみたことについて
迷っていることを整理してみるなら何かについて
言葉にするのが難しかった気持ちについて
思わず笑ってしまった出来事について
胸がいっぱいになった出来事について
今の自分を採点するなら何を基準にするかについて
買うか迷ったものと決め手について
失敗して初めてわかったことについて
うまくいったことの裏にあった準備について
待ち時間を自分の時間に変える方法について
覚えている夢から考えたことについて
会いたい人に今伝えたいことについて
連絡してよかったと思えたやり取りについて
行ってよかった場所で何が残ったかについて
聞いた話が心に残った理由について
日記を書くなら残したい一日について
ブログを書きたくなる瞬間について
文章を書くときのこだわりについて
写真を載せるなら選びたい一枚について
タイトルをつけるときに考えることについて
自分らしい文章とは何かについて
人のブログで好きなところについて
読みやすい記事だと思う条件について
コメントをもらってうれしい内容について
ネットで救われた経験について
オンラインとのちょうどいい距離感について
SNSで見かけて考えたことについて
通知を切りたくなる瞬間について
デジタルで便利になったことについて
アナログのほうが好きなことについて
紙に書きたいことについて
メモしておいて助かったことについて
忘れないようにしている工夫について
整理整頓でうまくいったことについて
散らかっていても困らない場所について
掃除したくなるきっかけについて
模様替えしたい場所について
引っ越すなら大事にしたい条件について
朝から調子がいい日は何が違うかについて
夜を気持ちよく終えるための工夫について
休日にこれだけできたら満足だと思うことについて
作業が進む場所に必要な条件について
歩いているだけで気分がよくなる道について
自分をねぎらうために選びたいことについて
待ち合わせまでの時間をどう楽しむかについて
もらってうれしかった贈り物の理由について
一人で過ごす時間に何を求めているかについて
誰かと過ごす時間で大切にしたいことについて
自分だけのランキングを作るなら何を並べるかについて
好きなものを三つ選ぶなら何を選ぶかについて
苦手なものを一つだけ減らせるなら何を選ぶかについて
少しだけ贅沢だと感じる時間について
今ならやり直したい小さな選択について
今日できたと言える小さな達成について
見慣れた場所で新しく気づいたことについて
`;

const originalTopicText = `
少しだけ勇気を出して試したことについて
誰かの親切で一日が軽くなった経験について
自分から少しだけ親切にできた経験について
人に理解されにくい好きなものについて
人にあまり言っていないこだわりについて
自分の中で決めているルールについて
ここぞという時の験担ぎについて
自分をゆるめるための方法について
集中したい時に最初にすることについて
自分にとってだけ特別な日について
よく歩く定番コースの楽しみについて
人とは少し違う楽しみ方について
買い物で大事にしている基準について
言葉の使い方で気をつけていることについて
方言や口ぐせにまつわる話について
名前にまつわる思い出について
あだ名にまつわる思い出について
初対面で見てしまうところについて
人と仲良くなるきっかけについて
会話が弾んだ話題について
沈黙が心地よかった相手について
誰かの影響で始めたことについて
影響を受けて好きになったものについて
誰かに教えてもらって助かったことについて
教えるなら何を一番伝えたいかについて
褒められてうれしいところについて
直したいけど直らない癖について
気づくと集まっているものについて
後回しにしがちなことを進める工夫について
気づくと見てしまう動画や記事について
立ち寄らずにいられない売り場について
選びがちな色や形の理由について
比べてしまう気持ちとの付き合い方について
考えすぎた時に抜け出す方法について
思わず笑ってしまう瞬間について
季節のイベントで好きな過ごし方について
誕生日をどう過ごしたいかについて
記念日に残したいことについて
贈り物を選ぶときに考えることについて
手紙を書くなら誰に何を書くかについて
写真を撮るときに大事にしていることについて
料理を作るときに楽しい工程について
買い物で失敗しないための工夫について
`;

const officialTopics = officialTopicText.trim().split(/\n+/).map((title) => ({ title, source: "official" }));
const originalTopics = originalTopicText.trim().split(/\n+/).map((title) => ({ title, source: "original" }));
const topics = [...officialTopics, ...originalTopics];

const canvas = document.getElementById("wheel");
const ctx = canvas.getContext("2d");
const spinButton = document.getElementById("spin-button");
const copyButton = document.getElementById("copy-button");
const reshuffleButton = document.getElementById("reshuffle-button");
const soundButton = document.getElementById("sound-button");
const result = document.getElementById("result");
const resultMeta = document.getElementById("result-meta");
const topicCount = document.getElementById("topic-count");

const palette = ["#e94f37", "#2b8c83", "#f6b83f", "#395b9d", "#8f5b9d", "#4d9f6f", "#d96c2c", "#2f7f9e"];
const volumeBoost = 2.5;
let reelTopics = [...topics];
let reelPosition = 0;
let currentTopic = null;
let spinning = false;
let soundEnabled = true;
let audioContext = null;
let flashPower = 0;
let particles = [];
const reelFrameImage = new Image();
let reelFrameReady = false;
reelFrameImage.onload = () => {
  reelFrameReady = true;
  drawSlot();
};
reelFrameImage.src = "assets/reel-frame-art.png";

topicCount.textContent = String(topics.length);

function getAudioContext() {
  if (!audioContext) {
    audioContext = new (window.AudioContext || window.webkitAudioContext)();
  }
  return audioContext;
}

function beep(frequency, duration, gain = 0.06, type = "sine") {
  if (!soundEnabled) return;
  const audio = getAudioContext();
  const oscillator = audio.createOscillator();
  const volume = audio.createGain();
  const boostedGain = Math.min(gain * volumeBoost, 0.32);
  oscillator.type = type;
  oscillator.frequency.value = frequency;
  volume.gain.setValueAtTime(0.0001, audio.currentTime);
  volume.gain.exponentialRampToValueAtTime(boostedGain, audio.currentTime + 0.01);
  volume.gain.exponentialRampToValueAtTime(0.0001, audio.currentTime + duration);
  oscillator.connect(volume).connect(audio.destination);
  oscillator.start();
  oscillator.stop(audio.currentTime + duration + 0.02);
}

function fanfare() {
  beep(392, 0.09, 0.09, "triangle");
  setTimeout(() => beep(523.25, 0.09, 0.09, "triangle"), 70);
  setTimeout(() => beep(659.25, 0.11, 0.1, "triangle"), 140);
  setTimeout(() => beep(783.99, 0.16, 0.1, "triangle"), 220);
  setTimeout(() => beep(1046.5, 0.18, 0.075, "sine"), 300);
}

function wrapIndex(index) {
  return ((index % reelTopics.length) + reelTopics.length) % reelTopics.length;
}

function topicAtPosition(position) {
  return reelTopics[wrapIndex(Math.round(position))];
}

function fitText(text, maxWidth, baseSize) {
  let size = baseSize;
  ctx.font = `900 ${size}px Yu Gothic, Meiryo, sans-serif`;
  while (ctx.measureText(text).width > maxWidth && size > 10) {
    size -= 2;
    ctx.font = `900 ${size}px Yu Gothic, Meiryo, sans-serif`;
  }
  return size;
}

function syncCanvasSize() {
  const rect = canvas.getBoundingClientRect();
  const width = Math.max(1, Math.round(rect.width));
  const height = Math.max(1, Math.round(rect.height));
  if (canvas.width === width && canvas.height === height) return;
  canvas.width = width;
  canvas.height = height;
}

function spawnParticles(count) {
  particles = Array.from({ length: count }, () => ({
    x: canvas.width * (0.16 + Math.random() * 0.72),
    y: canvas.height * (0.18 + Math.random() * 0.64),
    vx: (Math.random() - 0.5) * 18,
    vy: (Math.random() - 0.5) * 14 - 4,
    size: 5 + Math.random() * 12,
    life: 1,
    color: palette[Math.floor(Math.random() * palette.length)]
  }));
}

function drawParticles() {
  for (const particle of particles) {
    ctx.globalAlpha = Math.max(particle.life, 0);
    ctx.fillStyle = particle.color;
    ctx.beginPath();
    ctx.arc(particle.x, particle.y, particle.size, 0, Math.PI * 2);
    ctx.fill();
    particle.x += particle.vx;
    particle.y += particle.vy;
    particle.vy += 0.24;
    particle.life -= 0.022;
  }
  ctx.globalAlpha = 1;
  particles = particles.filter((particle) => particle.life > 0);
}

function drawSlot(position = reelPosition) {
  syncCanvasSize();
  const { width, height } = canvas;
  const centerX = width / 2;
  const centerY = height / 2;
  const rowHeight = height / 7.2;
  const base = Math.round(position);
  const drift = position - base;

  ctx.clearRect(0, 0, width, height);

  ctx.fillStyle = "rgba(255, 255, 255, .2)";
  for (let y = 24; y < height; y += 22) {
    for (let x = 28 + ((y / 22) % 2) * 10; x < width; x += 22) {
      ctx.beginPath();
      ctx.arc(x, y, 1.6, 0, Math.PI * 2);
      ctx.fill();
    }
  }

  ctx.save();
  ctx.translate(centerX, centerY);
  for (let row = -5; row <= 5; row += 1) {
    const distance = row - drift;
    const y = distance * rowHeight;
    const depth = Math.min(Math.abs(distance) / 5, 1);
    const scale = 1 - depth * 0.38;
    const alpha = 1 - depth * 0.58;
    const topic = reelTopics[wrapIndex(base + row)];
    const cardWidth = width * (0.72 - depth * 0.18);
    const cardHeight = rowHeight * (0.68 - depth * 0.1);
    const x = -cardWidth / 2 + depth * 44;
    const radius = 8;

    ctx.save();
    ctx.translate(0, y);
    ctx.scale(scale, scale);
    ctx.globalAlpha = alpha;
    const cardGradient = ctx.createLinearGradient(x, -cardHeight / 2, x + cardWidth, cardHeight / 2);
    cardGradient.addColorStop(0, row === 0 ? "rgba(255, 245, 199, .9)" : `${palette[wrapIndex(base + row) % palette.length]}cc`);
    cardGradient.addColorStop(0.45, row === 0 ? "rgba(255, 255, 255, .88)" : "rgba(255,255,255,.72)");
    cardGradient.addColorStop(1, row === 0 ? "rgba(255, 214, 79, .9)" : `${palette[wrapIndex(base + row + 3) % palette.length]}cc`);
    ctx.fillStyle = cardGradient;
    ctx.strokeStyle = row === 0 ? "#ff2d24" : "rgba(255, 255, 255, .84)";
    ctx.lineWidth = row === 0 ? 6 : 2;
    ctx.shadowColor = row === 0 ? "rgba(255, 45, 36, .82)" : "rgba(0, 0, 0, .2)";
    ctx.shadowBlur = row === 0 ? 18 : 5;
    ctx.beginPath();
    ctx.roundRect(x, -cardHeight / 2, cardWidth, cardHeight, radius);
    ctx.fill();
    ctx.stroke();
    ctx.shadowBlur = 0;

    ctx.fillStyle = row === 0 ? "#090d14" : "#23303d";
    ctx.textAlign = "center";
    ctx.textBaseline = "middle";
    const maxText = cardWidth - 36;
    const baseFontSize = row === 0
      ? Math.min(28, Math.max(12, rowHeight * 0.72))
      : Math.min(18, Math.max(10, rowHeight * 0.56));
    const fontSize = fitText(topic.title, maxText, baseFontSize);
    ctx.font = `900 ${fontSize}px Yu Gothic, Meiryo, sans-serif`;
    ctx.shadowColor = row === 0 ? "rgba(255,255,255,.95)" : "rgba(255,255,255,.86)";
    ctx.shadowBlur = row === 0 ? 1 : 2;
    ctx.fillText(topic.title, 0, 0);
    ctx.restore();
  }
  ctx.restore();

  ctx.shadowColor = "rgba(255, 45, 36, .9)";
  ctx.shadowBlur = 18;
  ctx.fillStyle = "rgba(255, 45, 36, .12)";
  ctx.fillRect(0, centerY - rowHeight * .34, width, rowHeight * .68);
  ctx.strokeStyle = "#ff2d24";
  ctx.lineWidth = 4;
  ctx.beginPath();
  ctx.moveTo(0, centerY);
  ctx.lineTo(width, centerY);
  ctx.stroke();
  ctx.shadowBlur = 0;

  for (let i = 0; i < 18; i += 1) {
    const bulbX = 34 + i * ((width - 68) / 17);
    const phase = (i + Math.floor(position * 3)) % 3;
    ctx.fillStyle = phase === 0 ? "#fff8a4" : phase === 1 ? "#ff2d24" : "#37e6ff";
    ctx.shadowColor = ctx.fillStyle;
    ctx.shadowBlur = 18;
    ctx.beginPath();
    ctx.arc(bulbX, 34, 6, 0, Math.PI * 2);
    ctx.arc(bulbX, height - 34, 6, 0, Math.PI * 2);
    ctx.fill();
  }
  ctx.shadowBlur = 0;

  if (flashPower > 0) {
    ctx.globalAlpha = flashPower;
    ctx.fillStyle = "#fff7a6";
    ctx.fillRect(0, 0, width, height);
    ctx.globalAlpha = 1;
    flashPower *= 0.88;
  }

  drawParticles();
}

function shuffleTopics() {
  for (let i = reelTopics.length - 1; i > 0; i -= 1) {
    const j = Math.floor(Math.random() * (i + 1));
    [reelTopics[i], reelTopics[j]] = [reelTopics[j], reelTopics[i]];
  }
  reelPosition = 0;
  drawSlot();
  setResult(topicAtPosition(reelPosition));
}

function easeOutCubic(t) {
  return 1 - Math.pow(1 - t, 3);
}

function setResult(topic) {
  currentTopic = topic;
  result.textContent = topic.title;
  resultMeta.textContent = "年に依存せず、記事に広げやすいブログ向けお題です。";
}

function spin() {
  if (spinning) return;
  spinning = true;
  spinButton.disabled = true;
  document.body.classList.add("jackpot");
  flashPower = 0.95;
  spawnParticles(120);
  result.textContent = "抽選中...";
  resultMeta.textContent = "中央の赤いラインが指したお題で止まります。";

  const targetIndex = Math.floor(Math.random() * reelTopics.length);
  const start = reelPosition;
  const startWhole = Math.round(start);
  const loops = 3 + Math.floor(Math.random() * 3);
  const delta = wrapIndex(targetIndex - startWhole);
  const end = startWhole + loops * reelTopics.length + delta;
  const duration = 4200 + Math.random() * 900;
  const startedAt = performance.now();
  let lastTick = Math.round(start);

  fanfare();

  function frame(now) {
    const progress = Math.min((now - startedAt) / duration, 1);
    const eased = easeOutCubic(progress);
    reelPosition = start + (end - start) * eased;
    drawSlot(reelPosition);

    const tick = Math.round(reelPosition);
    if (tick !== lastTick && progress < 0.96) {
      lastTick = tick;
      beep(760 + (tick % 10) * 38, 0.026, 0.045, "square");
      if (tick % 8 === 0) spawnParticles(16);
    }

    if (progress < 1) {
      requestAnimationFrame(frame);
      return;
    }

    reelPosition = end % reelTopics.length;
    drawSlot(reelPosition);
    const selected = topicAtPosition(reelPosition);
    spinning = false;
    spinButton.disabled = false;
    setResult(selected);
    flashPower = 0.8;
    spawnParticles(160);
    fanfare();
    setTimeout(() => document.body.classList.remove("jackpot"), 1600);
  }

  requestAnimationFrame(frame);
}

spinButton.addEventListener("click", spin);

copyButton.addEventListener("click", async () => {
  if (!currentTopic) return;
  await navigator.clipboard.writeText(`今週のお題「${currentTopic.title}」`);
  resultMeta.textContent = "コピーしました。";
  beep(880, 0.08, 0.05, "sine");
});

reshuffleButton.addEventListener("click", () => {
  shuffleTopics();
  resultMeta.textContent = "候補を更新しました。中央の赤いラインが指しているお題です。";
  beep(392, 0.08, 0.05, "triangle");
});

soundButton.addEventListener("click", () => {
  soundEnabled = !soundEnabled;
  soundButton.textContent = soundEnabled ? "音 ON" : "音 OFF";
  soundButton.setAttribute("aria-pressed", String(soundEnabled));
  if (soundEnabled) beep(660, 0.08, 0.05, "sine");
});

window.addEventListener("resize", () => {
  drawSlot();
});

shuffleTopics();
